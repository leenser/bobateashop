# Database Configuration
# Copy this file to .env and fill in your actual credentials

# PostgreSQL Connection (matches Java implementation)
DB_HOST=csce-315-db.engr.tamu.edu
DB_NAME=gang_81_db
DB_USER=your_username_here
DB_PASSWORD=your_password_here

# Alternative: Use full DATABASE_URL instead of individual components
# DATABASE_URL=postgresql://user:password@csce-315-db.engr.tamu.edu/gang_81_db

# Flask Configuration
# Valid values: development, dev, production, prod (defaults to dev)
FLASK_ENV=development
FLASK_APP=app

# Google Translate API Configuration
# Option 1: Use service account JSON file (recommended)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# Option 2: Use Google Cloud Project ID (requires gcloud authentication)
# GOOGLE_CLOUD_PROJECT=your-project-id

# Note: If neither is set, the app will use fallback dictionary-based translations
# for common tea product names

# For local development with SQLite (if no PostgreSQL credentials provided)
# The app will fall back to SQLite if DB_USER and DB_PASSWORD are not set
